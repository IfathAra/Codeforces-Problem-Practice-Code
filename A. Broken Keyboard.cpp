#include<bits/stdc++.h>
#include<math.h>
#define ll long long int
using namespace std;

int fmain()
{
    string s,res;
    int n,c=0,;
cin>>s;
//s[n]='9';
n=s.length();
if(s[0]!=s[1])
for(int i=1;i<n;i++)
{
    if(s[i]==s[i-1]) c++;
    else
    {
        if(c&1)
        {
            res
        }
    }
}
    return 0;
}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin>>t;
    while(t--)
    {
        fmain();
    }
}

